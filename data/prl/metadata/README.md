# PRL metadata

Place the cleaned local metadata file here as:

```text
prl_dataset_clean.xlsx
```

The expected fields include image name, date, time/hour, temperature, relative humidity, solar intensity, visibility, latitude, longitude, and distance.

The metadata itself is ignored by Git until its publication and privacy status has been confirmed.

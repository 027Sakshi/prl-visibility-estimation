# Experiments

`experiments/legacy/` contains exploratory entry points from the earlier SkyFinder and PCA/XGBoost development stages. They are retained for provenance but are not the canonical PRL pipeline.

The canonical current entry point is:

```text
run_prl_pipeline.py
```
